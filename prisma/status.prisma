model Status {
  id          String   @id @default(cuid())
  name        String   @unique
  state       Boolean
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@map("status")
}